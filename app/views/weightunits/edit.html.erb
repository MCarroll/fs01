<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3"><%= t("shared.scaffold.edit.title", model: "Weightunit") %></h1>
      <%= link_to t("cancel"), @weightunit, class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded shadow p-8">
      <%= render "form", weightunit: @weightunit %>
    </div>
  </div>
</div>
